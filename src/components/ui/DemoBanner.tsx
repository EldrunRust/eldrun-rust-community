'use client'

interface DemoBannerProps {
  isDemoMode?: boolean
  isSimulation?: boolean
}

export default function DemoBanner({ isDemoMode = false, isSimulation = false }: DemoBannerProps) {
  return null
}
